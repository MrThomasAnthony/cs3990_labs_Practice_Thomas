import { createButtons, displayButtonsWithDelay, createColorButton, showColorButton } from './myFunctions.js';

// Create buttons
const arrButtons = createButtons();

// Display buttons with a 1-second delay
createColorButton();
displayButtonsWithDelay(arrButtons, 1000);
