import { createButtons, displayButtonsWithDelay, createAndDisplayColorButton } from './myFunctions.js';

// Create buttons
const arrButtons = createButtons();

// Display buttons with a 1-second delay
displayButtonsWithDelay(arrButtons, 1000);

// Create and display a ColorButton
createAndDisplayColorButton();      